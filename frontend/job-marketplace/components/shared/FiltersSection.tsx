interface Props {
    className ?: string;
}

export const FiltersSection: React.FC<Props> = ({ className }) => {
    return (
        <>  
            <div className="relative w-full mt-12">
            </div>
        </>
    )
}