/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['jooblyua.s3.amazonaws.com',
            'encrypted-tbn0.gstatic.com',
            'companieslogo.com',
            'p.djinni.co'
        ],
    },
};

export default nextConfig;
